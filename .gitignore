# Data that appears by itself; never version these (see docs/FULL_EXECUTION_PLAN.md)
*.tiff
*.tif
*.nc
cpc_cache/
hazard_grid.csv
hazard_grid_meta.json
heat_grid.csv
heat_grid_meta.json
results_pack.json
results_pack_meta.json

# Environments and caches
.venv/
__pycache__/
*.pyc
.DS_Store
node_modules/

# Archive of superseded originals (kept local, per the reconciliation plan)
archive/
